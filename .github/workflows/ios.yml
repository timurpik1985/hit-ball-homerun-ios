steps:
  - uses: actions/checkout@v4

  - uses: actions/setup-node@v4
    with:
      node-version: 20

  - run: npm install

  - run: npm install -g cordova

  - run: cordova telemetry off   # or remove entirely

  - run: cordova build
