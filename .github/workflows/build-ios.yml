name: Build and Sign iOS App
on:
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Apple Certificate
        env:
          BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
        run: |
          CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          echo "$BUILD_CERTIFICATE_BASE64" | tr -d '\n ' | base64 --decode --output "$CERTIFICATE_PATH"
          security create-keychain -p "buildpassword" "$KEYCHAIN_PATH"
          security unlock-keychain -p "buildpassword" "$KEYCHAIN_PATH"
          security import "$CERTIFICATE_PATH" -k "$KEYCHAIN_PATH" -P 1234 -T /usr/bin/codesign

      - name: Install Provisioning Profile
        env:
          BUILD_PROVISION_PROFILE_BASE64: ${{ secrets.BUILD_PROVISION_PROFILE_BASE64 }}
        run: |
          PP_PATH=$RUNNER_TEMP/build_pp.mobileprovision
          echo "$BUILD_PROVISION_PROFILE_BASE64" | tr -d '\n ' | base64 --decode --output "$PP_PATH"
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp "$PP_PATH" ~/Library/MobileDevice/Provisioning\ Profiles/

      - name: System Environment Setup
        run: npm install -g cordova@12.0.0 --force

      - name: Prepare Platform
        run: |
          cordova platform add ios --non-interactive
          
      - name: Force iPad Compliance
        run: |
          # This command manually injects the "Full Screen" requirement into the iOS project
          # so Apple stops asking for Landscape orientations.
          PLIST="platforms/ios/Hit Ball Homerun/Hit Ball Homerun-Info.plist"
          if [ -f "$PLIST" ]; then
            /usr/libexec/PlistBuddy -c "Add :UIRequiresFullScreen bool true" "$PLIST" || /usr/libexec/PlistBuddy -c "Set :UIRequiresFullScreen true" "$PLIST"
            echo "Successfully patched Info.plist for iPad multitasking."
          else
            echo "Warning: Info.plist not found at $PLIST. Checking alternate path..."
            # Check for generic name if project name has spaces
            PLIST_ALT=$(find platforms/ios -name "*-Info.plist" | head -n 1)
            /usr/libexec/PlistBuddy -c "Add :UIRequiresFullScreen bool true" "$PLIST_ALT" || /usr/libexec/PlistBuddy -c "Set :UIRequiresFullScreen true" "$PLIST_ALT"
          fi

      - name: Build Signed App
        run: |
          cordova build ios --release --device --buildConfig=build.json --non-interactive

      - name: Locate and Verify IPA
        id: find_ipa
        run: |
          IPA_PATH=$(find platforms/ios -name "*.ipa" | head -n 1)
          echo "ipa_path=$IPA_PATH" >> $GITHUB_OUTPUT

      - name: Upload to TestFlight
        uses: apple-actions/upload-testflight-build@v1
        with:
          app-path: "${{ steps.find_ipa.outputs.ipa_path }}"
          issuer-id: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          api-key-id: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          api-private-key: ${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY }}
