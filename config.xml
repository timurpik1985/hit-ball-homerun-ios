name: Build iOS App
on:
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          # Forces the runner to look for the latest v22
          check-latest: true 

      - name: Install Cordova
        # The --force flag skips the engine warnings you're seeing
        run: npm install -g cordova@latest --force

      - name: Build iOS
        # This ENV block tells Cordova to stay quiet without using a broken command
        env:
          CORDOVA_TELEMETRY_OPT_OUT: 1
        run: |
          cordova platform add ios
          cordova build ios
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-app
          path: platforms/ios/build/
