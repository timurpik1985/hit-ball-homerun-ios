name: Build iOS App
on:
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          check-latest: true

      - name: Install Cordova
        # We use --force to bypass those 'Unsupported engine' warnings
        run: npm install -g cordova@latest --force

      - name: Build iOS
        # This ENV block is the modern way to disable telemetry. 
        # It won't crash the build because it's not a command.
        env:
          CORDOVA_TELEMETRY_OPT_OUT: 1
        run: |
          cordova platform add ios
          cordova build ios
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-app
          path: platforms/ios/build/
